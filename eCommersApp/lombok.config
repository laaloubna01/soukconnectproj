# This file is placed in your project root
# Tell Lombok to add @Generated annotation to generated methods
lombok.addLombokGeneratedAnnotation = true
# Make sure Lombok is fully enabled
lombok.anyConstructor.addConstructorProperties = true
# Enable all Lombok features
lombok.accessors.chain = true
# Ensure lombok operates in the correct directory
config.stopBubbling = true